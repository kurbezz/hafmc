# HafMC - Linktree Site

Статический сайт со ссылками для стримера HafMC.

## 🚀 Быстрый старт

### Локальная разработка

```bash
# Сборка Docker образа
docker build -t hafmc .

# Запуск контейнера
docker run -d -p 8080:80 --name hafmc hafmc

# Сайт будет доступен на http://localhost:8080
```

### Остановка контейнера

```bash
docker stop hafmc
docker rm hafmc
```

## 📦 Docker образ

Docker образ автоматически собирается при каждом push в ветку `main` через GitHub Actions и публикуется в GitHub Container Registry.

### Использование образа из GitHub Container Registry

```bash
# Запуск образа
docker run -d -p 8080:80 ghcr.io/kurbezz/hafmc:latest
```

## 🛠️ Структура проекта

```
hafmc/
├── .github/
│   └── workflows/
│       └── docker.yml      # GitHub Actions для сборки
├── www/                    # Статические файлы
│   ├── index.html
│   └── avatar.png
├── Dockerfile              # Docker конфигурация
├── nginx.conf              # Nginx конфигурация
└── README.md
```

## 🔧 Технологии

- **Nginx** - веб-сервер
- **Docker** - контейнеризация
- **GitHub Actions** - CI/CD

## 📝 Лицензия

© 2026 HafMC
